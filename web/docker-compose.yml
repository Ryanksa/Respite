version: "3.8"
services:
  web:
    image: web
    container_name: web
    ports:
      - "3000:3000"

  redis:
    image: redis
    container_name: redis
    command: redis-server --appendonly yes
    ports:
        - "6379:6379"
    volumes:
        - ./redis-volume:/data
